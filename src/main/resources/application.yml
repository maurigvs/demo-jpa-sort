spring:
  datasource:
    url: jdbc:postgresql://localhost:2222/demo_sort_db
    username: maestro
    password: maestropw
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
#      data-locations: classpath:starter_data.sql
#      mode: always
